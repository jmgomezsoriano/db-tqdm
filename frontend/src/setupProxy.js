module.exports = function (app) {
    // app.use('/api/bar/:bar_name', (req, res) => {
    //     const file = require(`../stubs/bar.json`);
    //     res.json(file);
    // });
    // app.use('/api/:page', (req, res) => {
    //     const file = require(`../stubs/${req.params.page}.json`);
    //     if (file) {
    //         console.log(`../stubs/${req.params.page}.json`);
    //         res.json(file);
    //     } else {
    //         res.sendStatus(404);
    //     }
    // });
};
